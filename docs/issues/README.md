# Project issue log

This directory records build, test, tooling, and dependency blockers encountered
while working on this project. Each issue includes evidence, impact, a verifiable
acceptance criterion, and a dated work log.

Statuses move from `open` to `in-progress` to `closed`; use `blocked` when an
external change is required before work can continue.
